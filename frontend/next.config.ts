import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Hide the on-screen Next.js dev indicator (the "N" route badge). Dev-only,
  // but distracting on a recorded demo.
  devIndicators: false,
};

export default nextConfig;
